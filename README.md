# 🍌 BananaCMS

<div align="center">

![GitHub stars](https://img.shields.io/github/stars/bananacms/BananaCMS?style=flat-square&logo=github)
![GitHub forks](https://img.shields.io/github/forks/bananacms/BananaCMS?style=flat-square&logo=github)
![GitHub issues](https://img.shields.io/github/issues/bananacms/BananaCMS?style=flat-square&logo=github)
![GitHub license](https://img.shields.io/github/license/bananacms/BananaCMS?style=flat-square)
![PHP Version](https://img.shields.io/badge/PHP-8.0+-777BB4?style=flat-square&logo=php&logoColor=white)
![MySQL Version](https://img.shields.io/badge/MySQL-5.7+-4479A1?style=flat-square&logo=mysql&logoColor=white)

</div>

<div align="right">
  <a href="README.en.md">English</a> | <strong>中文</strong>
</div>

轻量级影视内容管理系统，原生 PHP 开发，无框架依赖，开箱即用。

## ✨ 核心特性

- 🚀 **轻量高效** - 原生 PHP 8.0+，无框架依赖，性能优异
- 📦 **开箱即用** - 安装向导，5分钟部署上线
- 🎬 **智能采集** - 对接资源站 API，自动采集入库，定时任务
- 📱 **短视频/短剧** - 竖屏滑动播放，剧集管理，随机推荐
- 💬 **高级评论** - 楼中楼回复，点赞踩，敏感词过滤，评论审核
- ⭐ **评分系统** - 1-10分评分，统计分布，评分趋势
- 📢 **广告系统** - 10个广告位，4种广告类型，点击统计
- 📊 **数据统计** - UV/PV趋势，来源分析，热门排行，实时监控
- 🔌 **完整API** - 40+ RESTful 接口，Token认证，支持 APP 开发
- ☁️ **云存储** - 支持 Cloudflare R2，本地/云端双存储
- ⚡ **Redis缓存** - 高速缓存和Session，文件/Redis双驱动
- 🔒 **安全防护** - SQL注入/XSS/CSRF防护，管理员IP混淆
- 🎨 **模板系统** - 自定义标签，5种URL模式，SEO优化
- 🔄 **视频转码** - FFmpeg转码，进度跟踪，转码广告

## 📋 环境要求

- PHP 8.0+
- MySQL 5.7+
- PDO 扩展

可选：
- Redis 扩展（高速缓存）
- cURL 扩展（采集/云存储）

## 🚀 快速安装

1. 下载代码到网站目录
2. 访问 `http://你的域名/install.php`
3. 按向导填写数据库信息、管理员账号、自定义后台入口路径
4. 完成！访问你设置的后台路径进入管理后台

## 📁 目录结构

```
├── index.php           # 前台入口
├── admin.php           # 后台入口（安装时可自定义文件名）
├── api.php             # API入口
├── install.php         # 安装向导
├── cron.php            # 定时任务
├── config/             # 配置文件
├── core/               # 核心类库
├── models/             # 数据模型
├── controllers/        # 控制器
├── views/              # 后台视图
├── template/           # 前台模板
├── static/             # 静态资源
├── upload/             # 上传目录
└── runtime/            # 缓存/日志
```


## 🎯 功能清单

### 前台功能
- ✅ **首页系统** - 推荐/最新/热门视频，分类聚合，轮播图
- ✅ **视频系统** - 分类筛选，详情播放，自动连播，播放记录
- ✅ **短视频/短剧** - 竖屏滑动，剧集管理，随机推荐，分类浏览
- ✅ **演员系统** - 演员列表，演员详情，作品展示，演员搜索
- ✅ **文章系统** - 文章列表，文章详情，文章分类，文章搜索
- ✅ **搜索功能** - 全站搜索，热门搜索，搜索建议，搜索历史
- ✅ **用户系统** - 注册登录，个人中心，资料修改，密码修改
- ✅ **互动功能** - 评论回复，视频评分，点赞踩，收藏历史
- ✅ **友情链接** - 链接展示，链接申请，链接分类
- ✅ **单页面** - 关于我们，联系方式，免责声明，自定义页面

### 后台功能
- ✅ **仪表盘** - 数据概览，今日统计，系统状态，快捷操作
- ✅ **视频管理** - 视频CRUD，批量操作，播放源管理，视频锁定
- ✅ **分类管理** - 树形结构，分类CRUD，批量操作，分类排序
- ✅ **演员管理** - 演员信息，作品关联，演员统计，批量导入
- ✅ **文章管理** - 文章发布，编辑器，文章分类，文章统计
- ✅ **用户管理** - 用户列表，用户编辑，用户禁用，用户统计
- ✅ **采集管理** - 采集站配置，采集任务，采集日志，定时采集
- ✅ **广告管理** - 广告位配置，广告投放，点击统计，广告排序
- ✅ **评论管理** - 评论审核，评论删除，敏感词配置，评论统计
- ✅ **短视频管理** - 短视频/短剧管理，分集管理，状态控制
- ✅ **数据统计** - UV/PV趋势，来源分析，热门排行，数据导出
- ✅ **转码管理** - 转码任务，转码进度，转码广告，转码统计
- ✅ **友链管理** - 友链审核，链接检查，链接分类，链接统计
- ✅ **操作日志** - 管理员操作，日志查询，日志清理，安全审计
- ✅ **系统配置** - 站点信息，SEO配置，缓存配置，存储配置
- ✅ **播放器管理** - 播放器配置，播放器启用，播放器统计
- ✅ **单页管理** - 页面创建，页面编辑，页面删除，页面排序

### 技术特性
- ✅ **SEO优化** - sitemap生成，robots配置，自定义URL，meta标签
- ✅ **5种URL模式** - 支持slug伪静态，自定义规则，SEO友好
- ✅ **双缓存驱动** - 文件缓存/Redis缓存，缓存预热，缓存清理
- ✅ **双存储驱动** - 本地存储/Cloudflare R2，文件管理，CDN加速
- ✅ **安全防护** - CSRF/XSS/SQL注入防护，IP混淆，操作审计
- ✅ **RESTful API** - 40+接口，Token认证，APP支持，接口文档
- ✅ **模板系统** - 自定义标签，模板编译，变量渲染，模板缓存
- ✅ **多语言支持** - 中英文界面，多语言模板，国际化配置

## 📥 采集功能

支持对接资源站 API（JSON/XML），自动采集视频入库。

### 采集特性
- 🔄 **自动采集** - 定时任务，增量更新，断点续传
- 🎯 **分类映射** - 采集站分类自动映射到本站分类
- 📊 **采集统计** - 采集进度，成功率，错误日志
- ⚙️ **灵活配置** - 采集间隔，采集数量，采集规则
- 🔍 **采集日志** - 详细日志，错误追踪，性能监控

### 定时采集配置

```bash
# 每小时采集一次（推荐）
0 * * * * php /www/site/cron.php collect --hours=1

# 每6小时采集一次
0 */6 * * * php /www/site/cron.php collect --hours=6

# 每天凌晨2点采集
0 2 * * * php /www/site/cron.php collect --hours=24
```

### 采集站配置示例

```json
{
  "name": "资源站名称",
  "api": "https://api.example.com/api.php/provide/vod/",
  "token": "your_token_here",
  "categories": {
    "1": "电影",
    "2": "电视剧",
    "3": "综艺",
    "4": "动漫"
  }
}
```

### 资源站合作

> 📢 资源站合作联系 Telegram: [@ddys_io](https://t.me/ddys_io)

## 🔧 配置说明

> 配置文件 `config/config.php` 在安装时自动生成，包含数据库连接、站点信息等基础配置。以下为可选的高级配置。

### 缓存配置

```php
// 缓存驱动（file 或 redis）
define('CACHE_DRIVER', 'file');

// Session驱动（file 或 redis）
define('SESSION_DRIVER', 'file');
```

### Redis配置（可选）

```php
// Redis配置（当CACHE_DRIVER或SESSION_DRIVER为redis时生效）
define('REDIS_HOST', '127.0.0.1');
define('REDIS_PORT', 6379);
define('REDIS_PASS', '');           // Redis密码，无密码留空
define('REDIS_DB', 0);              // 缓存使用的数据库
define('REDIS_SESSION_DB', 1);      // Session使用的数据库
define('REDIS_PREFIX', 'xpk:');     // 缓存键前缀
```

### Cloudflare R2配置（可选）

```php
// 存储驱动（local 或 r2）
define('STORAGE_DRIVER', 'local');

// Cloudflare R2 配置（当STORAGE_DRIVER为r2时生效）
define('R2_ACCOUNT_ID', '');        // Cloudflare Account ID
define('R2_ACCESS_KEY_ID', '');     // R2 Access Key ID
define('R2_SECRET_ACCESS_KEY', ''); // R2 Secret Access Key
define('R2_BUCKET', '');            // Bucket名称
define('R2_PUBLIC_URL', '');        // 公开访问域名
```

### 安全配置

```php
// 管理员IP混淆盐值（安装时自动生成）
define('ADMIN_IP_SALT', 'your_random_salt_here');

// 安全密钥（安装时自动生成）
define('APP_SECRET', 'your_app_secret_here');

// 调试模式（生产环境请设为false）
define('APP_DEBUG', false);
```

## 🌐 伪静态配置

### Nginx配置（宝塔面板）

在宝塔面板的站点设置 → 伪静态中添加：

```nginx
# Sitemap
location = /sitemap.xml {
    rewrite ^ /sitemap.php last;
}

# 禁止访问敏感目录
location ~ ^/(config|core|models|controllers|views|runtime)/ {
    deny all;
}

# 前台伪静态
location / {
    try_files $uri $uri/ /index.php?s=$uri&$args;
}
```

### Apache配置

创建 `.htaccess` 文件：

```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Sitemap
    RewriteRule ^sitemap\.xml$ sitemap.php [QSA,L]

    # 前台路由
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?s=$1 [QSA,L]
</IfModule>

# 禁止访问敏感目录
<FilesMatch "^(config|core|models|controllers|views|runtime)">
    Order deny,allow
    Deny from all
</FilesMatch>
```

### URL模式说明

系统支持5种URL模式，可在后台配置：

1. **模式1**: `/vod/detail/123` (ID模式)
2. **模式2**: `/vod/123.html` (ID+HTML)
3. **模式3**: `/video/123` (自定义前缀)
4. **模式4**: `/video/movie-name` (Slug无后缀)
5. **模式5**: `/video/movie-name.html` (Slug+HTML)

> 完整配置文件见 `伪静态/` 目录

## 📱 API 文档

BananaCMS 提供完整的 RESTful API，支持开发原生 APP、小程序、第三方客户端。

详细文档请参考 [接口文档.md](接口文档.md)。

### 快速概览

- 基础地址: `/api.php`
- 返回格式: `{ "code": 0, "msg": "success", "data": {...} }`
- 认证方式: 请求头 `X-Token: {token}`

### 接口统计

| 分类 | 接口数 | 说明 |
|------|--------|------|
| 系统 | 3 | config, init, home |
| 用户 | 6 | 注册、登录、资料管理 |
| 视频 | 6 | 列表、详情、播放、热门、最新 |
| 分类 | 2 | 分类树、分类视频 |
| 演员 | 2 | 列表、详情 |
| 文章 | 2 | 列表、详情 |
| 搜索 | 3 | 搜索、热词、建议 |
| 收藏 | 4 | 列表、添加、删除、检查 |
| 历史 | 4 | 列表、添加、删除、清空 |
| 评论 | 4 | 列表、发表、删除、投票 |
| 评分 | 2 | 提交、统计 |
| 短视频 | 5 | 列表、详情、点赞、剧集、随机 |
| 广告 | 3 | 获取、点击、展示 |
| 友链 | 2 | 列表、申请 |
| 单页 | 2 | 列表、详情 |
| **总计** | **50+** | |

### 错误码

| code | 说明 |
|------|------|
| 0 | 成功 |
| 1 | 通用错误 |
| 401 | 未登录/Token过期 |
| 404 | 资源不存在 |

## 🚀 部署指南

### 生产环境部署

1. **环境准备**
   ```bash
   # 安装PHP 8.0+
   sudo apt install php8.0 php8.0-fpm php8.0-mysql php8.0-curl php8.0-redis
   
   # 安装MySQL 5.7+
   sudo apt install mysql-server
   
   # 安装Nginx
   sudo apt install nginx
   ```

2. **代码部署**
   ```bash
   # 下载代码
   git clone https://github.com/bananacms/BananaCMS.git
   cd BananaCMS
   
   # 设置权限
   chmod -R 755 .
   chmod -R 777 upload/ runtime/
   ```

3. **配置文件**
   ```bash
   # 复制配置文件
   cp config/config.example.php config/config.php
   
   # 编辑配置
   nano config/config.php
   ```

4. **运行安装**
   ```bash
   # 访问安装向导
   https://yourdomain.com/install.php
   ```

5. **安全加固**
   ```bash
   # 删除安装文件
   rm install.php data.sql *.md
   
   # 设置只读权限
   chmod 644 config/config.php
   ```

### Docker部署

```dockerfile
FROM php:8.0-fpm-alpine

# 安装扩展
RUN docker-php-ext-install pdo_mysql

# 复制代码
COPY . /var/www/html

# 设置权限
RUN chmod -R 755 /var/www/html
RUN chmod -R 777 /var/www/html/upload /var/www/html/runtime

EXPOSE 9000
```

### 定时任务配置

```bash
# 编辑crontab
crontab -e

# 添加定时任务
# 每小时采集一次
0 * * * * php /www/site/cron.php collect --hours=1

# 每天清理日志
0 2 * * * php /www/site/cron.php clean --days=30

# 每天生成sitemap
0 3 * * * php /www/site/sitemap.php
```

## 🔧 常见问题

### 安装问题

**Q: 安装时提示数据库连接失败？**
A: 检查数据库配置信息，确保MySQL服务正常运行，用户权限正确。

**Q: 安装后无法访问后台？**
A: 检查伪静态配置是否正确，确保后台入口文件存在且有执行权限。后台路径在安装时自定义设置。

**Q: 上传文件失败？**
A: 检查upload目录权限是否为777，PHP上传限制是否足够大。

### 功能问题

**Q: 采集不工作？**
A: 检查cURL扩展是否安装，采集站API是否正常，定时任务是否配置。

**Q: 缓存不生效？**
A: 检查runtime目录权限，Redis连接是否正常，缓存配置是否正确。

**Q: 视频无法播放？**
A: 检查播放地址是否有效，播放器配置是否正确，HTTPS混合内容问题。

### 性能问题

**Q: 网站访问慢？**
A: 启用Redis缓存，配置CDN加速，优化数据库查询，启用Gzip压缩。

**Q: 数据库查询慢？**
A: 检查数据库索引，优化SQL查询，考虑读写分离，定期清理日志。

## 📈 升级指南

### 版本升级

1. **备份数据**
   ```bash
   # 备份数据库
   mysqldump -u root -p bananacms > backup.sql
   
   # 备份文件
   tar -czf backup.tar.gz upload/ config/
   ```

2. **下载新版本**
   ```bash
   # 下载新版本
   wget https://github.com/bananacms/BananaCMS/releases/latest
   ```

3. **覆盖文件**
   ```bash
   # 保留配置和上传文件
   cp -r upload/ upload_backup/
   cp config/config.php config_backup.php
   
   # 覆盖新文件
   # 恢复配置和上传文件
   ```

4. **数据库升级**
   ```bash
   # 运行升级脚本（如果有）
   php upgrade.php
   ```

### 数据迁移

从其他CMS迁移到BananaCMS：

1. **导出数据** - 从原系统导出视频、分类、用户数据
2. **格式转换** - 转换为BananaCMS数据格式
3. **导入数据** - 使用导入工具批量导入
4. **URL重定向** - 配置301重定向保持SEO

## 🤝 贡献指南

### 开发环境

1. **克隆项目**
   ```bash
   git clone https://github.com/bananacms/BananaCMS.git
   cd BananaCMS
   ```

2. **安装依赖**
   ```bash
   # 无需Composer，原生PHP项目
   ```

3. **配置环境**
   ```bash
   cp config/config.example.php config/config.php
   # 编辑配置文件
   ```

### 代码规范

- 遵循PSR-4自动加载规范
- 使用PSR-12代码风格
- 所有类和方法必须有注释
- 提交前运行测试

### 提交流程

1. Fork项目
2. 创建功能分支
3. 提交代码
4. 创建Pull Request

## 📞 技术支持

- **GitHub**: [https://github.com/bananacms/BananaCMS](https://github.com/bananacms/BananaCMS)
- **Telegram**: [@BananaCMS](https://t.me/BananaCMS)
- **邮箱**: banana@xpornkit.com

## 🛠 技术栈

| 组件 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 后端语言 | PHP | 8.0+ | 原生PHP，无框架依赖 |
| 数据库 | MySQL | 5.7+ | 支持InnoDB引擎 |
| 前端框架 | Tailwind CSS | 3.x | 响应式设计，移动端优化 |
| 缓存系统 | File / Redis | - | 双驱动，支持集群 |
| 存储系统 | Local / R2 | - | 本地存储/云存储 |
| 会话管理 | File / Redis | - | 支持分布式Session |
| 视频转码 | FFmpeg | 4.0+ | 可选，支持多格式转码 |

## 🔒 安全特性

### 数据安全
- **SQL注入防护** - PDO预处理语句，参数绑定
- **XSS防护** - 输出转义，HTML标签过滤
- **CSRF防护** - Token验证，表单保护
- **密码安全** - BCrypt哈希，盐值加密

### 访问控制
- **目录保护** - 敏感目录访问限制
- **文件保护** - 配置文件、日志文件保护
- **IP混淆** - 管理员IP地址混淆存储
- **会话安全** - HttpOnly Cookie，SameSite属性

### 审计日志
- **操作日志** - 管理员操作完整记录
- **登录日志** - 登录尝试，IP地址记录
- **采集日志** - 采集操作，错误追踪
- **搜索日志** - 搜索行为，热词统计

## 🎨 模板系统

BananaCMS 提供强大的模板标签系统，详细文档请参考 [模板变量.md](模板变量.md)。

### 快速示例

```html
<!-- 视频列表 -->
{xpk:vod num="10" order="time" type="1"}
  <div class="video-item">
    <h3>{$vo.vod_name}</h3>
    <img src="{$vo.vod_pic}" alt="{$vo.vod_name}">
  </div>
{/xpk:vod}

<!-- 分类列表 -->
{xpk:type}
  <a href="/type/{$vo.type_id}">{$vo.type_name}</a>
{/xpk:type}

<!-- 热门视频 -->
{xpk:hot num="10"}
  <div class="hot-video">{$vo.vod_name}</div>
{/xpk:hot}

<!-- 广告位 -->
{xpk:ad position="home_top"}

<!-- 页脚版权（必须保留） -->
{xpk:footer}
```

### 自定义模板

1. 复制 `template/default/` 目录
2. 重命名为你的模板名称
3. 修改模板文件
4. 在后台切换模板

## 📊 性能优化

### 缓存策略
- **页面缓存** - 首页、列表页静态化
- **数据缓存** - 热门数据Redis缓存
- **模板缓存** - 模板编译缓存
- **图片缓存** - CDN加速，浏览器缓存

### 数据库优化
- **索引优化** - 关键字段建立索引
- **查询优化** - 避免N+1查询
- **分页优化** - 大数据量分页优化
- **连接池** - 数据库连接复用

### 前端优化
- **资源压缩** - CSS/JS文件压缩
- **图片优化** - WebP格式，懒加载
- **CDN加速** - 静态资源CDN分发
- **浏览器缓存** - 合理设置缓存头

## 📊 项目统计

| 类型 | 数量 | 说明 |
|------|------|------|
| 核心类库 | 11个 | Database, Router, Cache, Template等 |
| 数据模型 | 22个 | Vod, User, Comment, Score等完整模型 |
| 前台控制器 | 12个 | 首页、视频、用户等功能控制器 |
| 后台控制器 | 22个 | 完整的后台管理功能 |
| API接口 | 50+个 | 完整的RESTful API |
| 模板标签 | 10+个 | 自定义模板标签系统 |
| 数据表 | 25+个 | 完整的数据库结构 |
| 代码行数 | 48000+ | PHP/HTML/JS/CSS代码 |

## 📢 社区

- **Telegram**: [@BananaCMS](https://t.me/BananaCMS)
- **GitHub**: [BananaCMS](https://github.com/bananacms/BananaCMS)

## 📄 开源协议

MIT License - 自由使用，商业友好

---

**BananaCMS** - 比苹果更轻量，比橙子更甜美 🍌

Powered by [XPornKit成人导航](https://xpornkit.com/zh)